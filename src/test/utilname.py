from utils.utilname import generate_name

sdef test():
    import logging

    logging.debug(generate_name())
    logging.debug(generate_name())
    logging.debug(generate_name())
    logging.debug(generate_name())


test()
